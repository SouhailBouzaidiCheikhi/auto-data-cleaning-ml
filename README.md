# 🤖 Auto Data Cleaning & ML Web App

An end-to-end **CSV → Clean → Encode → Scale → Train → Predict → Excel** web app built with **Streamlit**.

## 🚀 Features
- Upload any CSV file
- Automatic data cleaning
- Scaling & One-Hot Encoding
- Automatic ML model training
- Classification & Regression support
- Excel export with predictions

## 🧪 Demo Dataset
Located in `data/demo_dataset.csv`

## ▶️ Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py
